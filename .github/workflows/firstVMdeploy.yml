name: build provisioningVM
on:
  push:

jobs:
  maven-build:
    name: Build provisioningVM
    runs-on: provisioningvm
    steps:
      - name: checkout code
        uses: actions/checkout@v4
      - name: where am I
        run: pwd
      - name: run terraform init
        run: terraform init
      - name: run terraform plan
        run: terraform plan
      - name: run terraform apply -auto-approve
        run: terraform apply -auto-approve
