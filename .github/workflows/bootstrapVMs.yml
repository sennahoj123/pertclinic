name: build provisioningVM
on: push

jobs:
  build-VM:
    name: Build provisioningVM
    runs-on: iede
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Terraform Init
        run: |
          cd terraform-azure
          terraform init

      - name: Terraform Apply
        run: |
          cd terraform-azure
          terraform apply -auto-approve

      - name: Output Public IP Addressesvim
        run: terraform output -json > /opt/pertclinic/terraform-azure/ip_addresses.json

      - name: Generate Ansible Inventory
        run: /opt/pertclinic/terraform-azure/generate_inventory.sh ip_addresses.json > hosts

      - name: testing playbook
        run: ansible-playbook -i hosts /opt/pertclinic/ansible/main-testing.yml

        
