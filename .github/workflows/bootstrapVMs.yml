name: build provisioningVM
on: push

jobs:
  build-VM:
    name: Build provisioningVM
    runs-on: iede
    
    steps:
      - name: chdir terraform
        run: |
          cd terraform-azure-provisioningvm
          pwd
          ll
      - name: terraform init
        run: terraform init
      - name: terraform plan
        run: terraform plan
      - name: terraform apply
        run: terraform apply -auto-appove
      - name: where am I
        run: |
          pwd
          ls -la
      - name: testing playbook
        run: ansible-playbook -i hosts ansible/main-testing.yml
        
